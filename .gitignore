.DS_Store

# GES-2 case: keep only the compressed derivatives, not the 47MB of source PNGs.
# Originals stay local; regenerate with the sharp script if they are ever needed.
assets/GES-2 digital design/*
!assets/GES-2 digital design/Сжатые/

# Margarita Pushkina page: same rule — only the compressed derivatives are tracked.
assets/Margarita Puskina/*
!assets/Margarita Puskina/Сжатые/

# A-club page: only the compressed derivatives are tracked.
assets/A Club/*
!assets/A Club/Сжатые/

# ÖMANKÖ ꞉DÖM page: only the compressed derivatives are tracked. The two Kiosk
# videos are copied into Сжатые/ as-is — no ffmpeg here to transcode them.
assets/Omanko dom/*
!assets/Omanko dom/Сжатые/

# Stray exports that live in the project folder but must not be published.
Downloads/
